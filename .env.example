# 仅供构建和部署进程读取。任何 VITE_* 均会进入浏览器产物，本项目不使用 VITE_* 保存配置或密钥。

# 生产模式必须显式设置以下路径；开发与 CI 自动使用 config/*.example.yaml 和 fixtures/content/。
FUYAO_CONFIG_ROOT=/srv/fuyao/shared/config
FUYAO_CONTENT_ROOT=/srv/fuyao/shared/content
FUYAO_RELEASE_ROOT=/srv/fuyao/releases
FUYAO_CURRENT_LINK=/srv/fuyao/current
FUYAO_SHARED_ROOT=/srv/fuyao/shared
FUYAO_DEPLOY_LOCK=/srv/fuyao/deploy.lock
FUYAO_SECRETS_FILE=/srv/fuyao/shared/secrets/secrets.env

# 代码部署从公开 main 分支获取源码。示例值不能用于生产发布。
FUYAO_REPOSITORY_URL=https://github.com/example/example.git
FUYAO_RELEASE_KEEP=5

# 可选：切换 current 后执行健康检查；失败时自动恢复旧指针。
FUYAO_HEALTHCHECK_URL=https://example.com/health

# 密钥不得写入本文件。生产密钥文件固定为 /srv/fuyao/shared/secrets/secrets.env，权限建议为 0600。
