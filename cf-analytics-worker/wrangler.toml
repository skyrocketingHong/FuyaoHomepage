name = "cf-analytics-worker"
main = "worker.js"
compatibility_date = "2024-01-01"

# 部署后通过以下命令配置 Secrets:
# wrangler secret put CF_API_TOKEN
# wrangler secret put CF_ZONE_ID

[vars]
# 允许跨域访问的域名（根据实际情况修改）
ALLOWED_ORIGIN = "https://fuyaoskyrocket.ing"
